@if (TempData.Peek("MensagemDeSucesso") != null)
{
    <script>
        var mensagem = "@TempData["MensagemDeSucesso"]";
        novoBanco.AdicionarMensagemDeSucesso(mensagem);
    </script>
}

@if (TempData.Peek("MensagemDeErro") != null)
{
    <script>
        var mensagem = "@TempData["MensagemDeErro"]";
        novoBanco.AdicionarMensagemDeErro(mensagem);
    </script>
}
